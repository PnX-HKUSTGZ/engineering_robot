# bringup

请将整个文件夹下载后使用！！

# 在现有的机器上使用

## 无交互

```bash
sudo chmod +x bringup.sh
/bin/bash -c "./bringup.sh -y"
```

## 交互

```bash
sudo chmod +x bringup.sh
/bin/bash -c "./bringup.sh -y"
```

# docker

```bash
sudo docker build -t rm_engneering .
```